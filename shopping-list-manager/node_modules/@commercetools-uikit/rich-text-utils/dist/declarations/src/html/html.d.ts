import { type Descendant, type BaseEditor, type BaseText } from 'slate';
import { type ReactEditor as TReactEditor } from 'slate-react';
import type { HistoryEditor } from 'slate-history';
import { BLOCK_TAGS, MARK_TAGS } from '../tags';
type Html = string;
export type CustomElement = {
    type: Format;
    children: CustomText[];
    align?: string;
};
type CustomText = BaseText & {
    bold?: boolean;
    code?: string;
    italic?: string;
    underline?: string;
    superscript?: string;
    subscript?: string;
    strikethrough?: string;
};
export type Format = (typeof BLOCK_TAGS)[keyof typeof BLOCK_TAGS] & (typeof MARK_TAGS)[keyof typeof MARK_TAGS];
declare module 'slate' {
    interface CustomTypes {
        Editor: BaseEditor & TReactEditor & HistoryEditor;
        Element: CustomElement;
        Text: CustomText;
    }
}
export type Deserialized = Descendant | null;
export declare const defaultSlateState: Descendant[];
declare const html: {
    serialize: (value: Deserialized | Deserialized[]) => Html;
    deserialize: (html: Html) => Deserialized | Deserialized[];
};
export default html;
