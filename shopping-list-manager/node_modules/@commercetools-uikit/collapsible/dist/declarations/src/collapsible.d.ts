import { ReactNode } from 'react';
type TToggleCallback = (toggleState?: boolean) => void;
export type TCollapsibleProps = {
    isDefaultClosed?: boolean;
    children: (options: {
        isOpen: boolean;
        toggle?: TToggleCallback;
    }) => ReactNode;
    isClosed?: boolean;
    onToggle?: TToggleCallback;
};
declare const Collapsible: {
    (props: TCollapsibleProps): import("@emotion/react/jsx-runtime").JSX.Element;
    displayName: string;
    defaultProps: Pick<TCollapsibleProps, "isDefaultClosed">;
};
export default Collapsible;
