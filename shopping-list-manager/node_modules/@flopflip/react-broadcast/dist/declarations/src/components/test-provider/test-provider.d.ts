import { type TAdapterIdentifiers, type TAdaptersStatus, type TFlags, type TReconfigureAdapter } from '@flopflip/types';
import React from 'react';
type TProps = {
    readonly children: React.ReactNode;
    readonly flags: TFlags;
    readonly adapterIdentifiers?: TAdapterIdentifiers[];
    readonly reconfigure?: TReconfigureAdapter;
    readonly status?: TAdaptersStatus;
};
declare function TestProvider(props: TProps): JSX.Element;
declare namespace TestProvider {
    var displayName: string;
    var defaultProps: Pick<TProps, "status" | "adapterIdentifiers" | "reconfigure">;
}
export { TestProvider };
