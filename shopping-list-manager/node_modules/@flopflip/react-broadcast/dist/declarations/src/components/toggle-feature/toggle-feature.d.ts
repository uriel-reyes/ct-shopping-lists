import { type TToggleFeatureProps } from '@flopflip/react';
import { type TFlagName, type TFlagVariation } from '@flopflip/types';
type Props = {
    flag: TFlagName;
    variation?: TFlagVariation;
} & Omit<TToggleFeatureProps, 'isFeatureEnabled'>;
declare function ToggleFeature<OwnProps extends Props>(props: OwnProps): JSX.Element;
declare namespace ToggleFeature {
    var displayName: string;
}
export default ToggleFeature;
