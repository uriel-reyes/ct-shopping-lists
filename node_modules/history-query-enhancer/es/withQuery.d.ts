import { History } from 'history';
import { EnhancedHistory, QueryTransformer } from '.';
declare const withQuery: ({ parse, stringify }: QueryTransformer) => (history: History) => EnhancedHistory;
export default withQuery;
