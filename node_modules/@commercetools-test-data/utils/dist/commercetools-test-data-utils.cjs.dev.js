'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

/**
 * Returns three ISO dates of different timestamps.
 *
 * Usage:
 *
 * ```js
 * const [getOlderDate, getNewerDate] = createRelatedDates();
 * const generator = Generator({
 *  fields: {
 *    createdAt: fake(getOlderDate),
 *    lastModifiedAt: fake(getNewerDate),
 *  },
 * });
 * ```
 */
var createRelatedDates = function createRelatedDates() {
  var recentDaysFromPastReference = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : 10;
  var pastReference = new Date();
  var getOlderDate = function getOlderDate(f) {
    return f.date.recent(recentDaysFromPastReference, pastReference.toISOString()).toISOString();
  };
  var getNewerDate = function getNewerDate(f) {
    return f.date.between(new Date(pastReference).toISOString(), new Date().toISOString()).toISOString();
  };
  var getFutureDate = function getFutureDate(f) {
    return f.date.future().toISOString();
  };
  return [getOlderDate, getNewerDate, getFutureDate];
};

exports.createRelatedDates = createRelatedDates;
