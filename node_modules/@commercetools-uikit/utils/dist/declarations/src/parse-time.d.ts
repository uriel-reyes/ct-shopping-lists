type ParsedTime = {
    hours: number;
    minutes: number;
    seconds: number;
    milliseconds: number;
};
declare const parseTime: (rawTime?: string) => ParsedTime | null;
export default parseTime;
