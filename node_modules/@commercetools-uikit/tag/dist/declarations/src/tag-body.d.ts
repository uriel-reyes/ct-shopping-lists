import type { TTagProps } from './tag';
import { ReactNode } from 'react';
import { Link } from 'react-router-dom';
export type TTagBodyProps = {
    to?: TTagProps['to'];
    as?: typeof Link;
    type?: TTagProps['type'];
    onClick?: TTagProps['onClick'];
    onRemove?: TTagProps['onRemove'];
    isDisabled?: boolean;
    isDraggable?: boolean;
    children: ReactNode;
    styles?: TTagProps['styles'];
};
declare const TagBody: {
    (props: TTagBodyProps): import("@emotion/react/jsx-runtime").JSX.Element;
    defaultProps: Pick<TTagProps, "type" | "isDisabled" | "isDraggable">;
    displayName: string;
};
export default TagBody;
